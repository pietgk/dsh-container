# Phase 3 counter fixture

This dependency-free application is copied into a disposable managed workspace
for controlled DSH evaluation. The tracked fixture remains unchanged by the
evaluation itself.

Run its tests with `npm test`.
